module provides the 'server' component

simple encode/decode for the wire format
simple socket connect
device, device host, 'autoconnect'
'autoconnect' being fairly buggy, but, working well enough for the moment
